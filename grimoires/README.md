# Legacy Grimoires

These files are legacy standalone microsite pages.
They are intentionally kept separate from the main Jekyll site and are excluded from the Jekyll build via `_config.yml`.

They are not converted to the shared `layout: default` structure because each file contains its own full HTML document, custom CSS, navigation, and page-level scripts.

If these are no longer needed, remove the `grimoires/` folder entirely.
